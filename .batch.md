# Batch: CLAUDE.md Current Work

## Task 1: Roblox package publish script
Wire darklua (already configured at `.darklua.json`, converts `@src/...` requires to relative paths) into an actual build step that produces a standalone Roblox-importable package.

Files to create:
- **scripts/BuildPackage.luau**: shells out to `darklua process src dist/lattice --config .darklua.json` (use Lune's `process.exec` or `process.spawn` — check `@lune/process` docs). Output dir: `dist/lattice/`. Print success/failure and file count.

Files to modify:
- **pesde.toml**: add `build-package = "scripts/BuildPackage.luau"` under `[scripts]`.
- **.gitignore**: add `dist/`.

Do NOT touch `src/`, `bench/`, or `docs/`. Verify by running `pesde run build-package` (or `lune run scripts/BuildPackage.luau`) from the worktree and confirming `dist/lattice/` is populated with `.luau` files containing relative (not `@src`) requires.

## Task 2: Benchmark profiling pass
Profile the existing pipeline benchmark (`bench/RunPipeline.luau`, `bench/Run.luau`, `bench/Fixtures.luau`, `bench/Report.luau`) and apply micro-optimizations to hot codec paths identified.

Approach:
1. Run `lune run bench/RunPipeline.luau` (or the pesde script `bench-pipeline`) to get baseline timings.
2. Read `src/serde/cframe.luau`, `src/serde/array.luau`, and `src/format/` bitmask packing code (identify exact file via grep for "bitmask" in `src/format/`) — these are the hot paths called out in CLAUDE.md.
3. Apply targeted micro-optimizations only where the profile shows real cost (e.g. avoid redundant table allocations, hoist repeated lookups, avoid closures in hot loops). Do not rewrite algorithms or change codec wire format.
4. Re-run the benchmark and confirm timings improved or are unchanged (never regressed).

Only touch files under `src/serde/` and `src/format/` that the benchmark shows as hot. Do NOT touch `bench/` files themselves or `src/artifacts/`.

## Task 3: User guide and end-to-end example doc
Write user-facing documentation for the serialize/deserialize API and artifact format.

Files to create:
- **docs/guide.md**: covers (a) loading an artifact via `src/artifacts/Artifact.luau`, (b) serializing/deserializing instances via `src/instance/` API, (c) extending the codec registry in `src/serde/init.luau` with a new codec. Pull real function signatures from those files — do not invent API shape.
- **docs/examples/end-to-end.md**: short walkthrough referencing the existing benchmark fixture pattern in `bench/Fixtures.luau` and `scripts/RoundTrip.luau` (load a Roblox place-like instance tree, serialize, read back, assert equality). Link to `docs/examples/` sibling docs (varint.md, rle.md, etc.) for format internals, don't duplicate them.

Do NOT modify any files under `src/`, `bench/`, or `scripts/`. Read-only research into those, doc-only output.
